hyper_parameters:
  n_epochs: 4
  lr: 1e-5
  batch_size: 32
  n_rows: 64

wandb:
  mode: "online" # "online", "offline" or "disabled". Disabled when gCloud
  project: "dtu_mlops_group10"
  entity: "dtu_mlops_group10"
  model_dir: '/src/models'
